# 🧹 Globals.css 清理计划

## 目标
删除所有已被 @genki/shared-ui 替代的冗余手工样式

## 需要删除的样式类

### 1. Tab 相关（已被 RootTabs 替代）
- `.tab-list` (line 227-233)
- `.tab-item` (line 236-256)
- `.panel-tabs` (line 695-698)
- `.panel-tab` (line 701-722)
- `.panel-tab-content` (line 725-733)

### 2. Button 相关（已被 Button 组件替代）
- `.secondary-btn` (line 456-477)
- `.btn-secondary` (line 162-170) - 保留，可能其他地方在用
- `.btn-primary` (line 149-160) - 保留
- `.btn-ghost` (line 172-180) - 保留

### 3. 保留的样式
- Design tokens (tokens-*.css)
- 全局基础样式 (html, body, #root)
- 滚动条样式
- 滑块样式
- Toggle 开关样式（FoldTab 还在用）
- Section 样式（布局相关）
- 工艺按钮样式（CraftTab 还在用）

## 执行步骤
1. 备份当前 globals.css
2. 删除冗余的 Tab 样式
3. 删除冗余的 Button 样式
4. 验证构建成功
5. 测试所有组件正常显示
