# 🔧 硬编码值优化方案

## 📊 问题分析

### 统计数据
- **总计硬编码值**: 227 处
- **涉及文件数**: 13 个组件文件
- **主要问题文件**:
  1. `canvas/ViewportArea.tsx` - 68 处
  2. `craft/CraftParamPanel.tsx` - 37 处
  3. `fold/FoldOrderPanel.tsx` - 23 处
  4. `panels/FoldTab.tsx` - 20 处
  5. `craft/CraftThumbnails.tsx` - 19 处

### 硬编码类型
1. **颜色值**: `rgba()`, `#hex`
2. **尺寸值**: `px`, `rem`
3. **阴影值**: `box-shadow`, `drop-shadow`
4. **动画值**: `cubic-bezier`, 时长

---

## 🎯 优化策略

### 方案 A：渐进式迁移（推荐）
**适用场景**: 现有项目，需要保持稳定性

**步骤**:
1. **第一阶段**: 补充缺失的 semantic tokens
2. **第二阶段**: 创建专用 CSS 类
3. **第三阶段**: 逐个组件迁移
4. **第四阶段**: 验证和测试

**优点**:
- 风险可控，可随时回滚
- 不影响现有功能
- 可分批次进行

**缺点**:
- 耗时较长
- 需要维护两套代码

### 方案 B：一次性重构
**适用场景**: 新项目或可以停止开发的项目

**步骤**:
1. 创建完整的 token 映射表
2. 批量替换所有硬编码值
3. 全面测试

**优点**:
- 一次性解决问题
- 代码更干净

**缺点**:
- 风险高，可能引入 bug
- 需要大量测试

---

## 📋 具体实施计划（方案 A）

### 第一阶段：补充 Semantic Tokens

需要补充的 tokens：

```css
/* 玻璃态效果 */
--glass-bg-light: rgba(255, 255, 255, 0.06);
--glass-bg-medium: rgba(255, 255, 255, 0.04);
--glass-bg-dark: rgba(10, 15, 20, 0.82);
--glass-border: rgba(255, 255, 255, 0.08);
--glass-blur: blur(18px);

/* 面板颜色 */
--panel-bg-primary: rgba(10, 10, 11, 1);
--panel-bg-secondary: rgba(15, 15, 18, 0.72);
--panel-border: rgba(255, 255, 255, 0.08);

/* 状态颜色（补充） */
--status-orange: #f59e0b;
--status-green: #10b981;
--status-purple: #6366f1;
--status-pink: #ec4899;
--status-teal: #14b8a6;
```

### 第二阶段：创建专用 CSS 类

在 `globals.css` 中添加：

```css
/* 玻璃态面板 */
.glass-panel {
  background: var(--glass-bg-dark);
  backdrop-filter: var(--glass-blur);
  border: 1px solid var(--glass-border);
  border-radius: var(--p-radius-md);
}

/* 浮动工具栏 */
.floating-toolbar {
  position: fixed;
  z-index: var(--p-z-popover);
  background: var(--panel-bg-secondary);
  border: 1px solid var(--panel-border);
}
```

### 第三阶段：优先级排序

**高优先级**（影响大，使用频繁）:
1. `canvas/ViewportArea.tsx` - 68 处
2. `craft/CraftParamPanel.tsx` - 37 处

**中优先级**（中等影响）:
3. `fold/FoldOrderPanel.tsx` - 23 处
4. `panels/FoldTab.tsx` - 20 处

**低优先级**（影响小）:
5. 其他组件

---

## 🎯 当前架构成果

### ✅ 已完成的工作

1. **100% Token 复用** - 260+ tokens 全部来自 shared-theme
2. **组件标准化** - Button, Slider 已迁移到 shared-ui
3. **Tailwind 优化** - 所有配置使用 token 变量
4. **三层架构** - Primitive → Semantic → Component

### 📊 架构完整度

| 层级 | 完成度 | 说明 |
|------|--------|------|
| Primitive Tokens | 100% | 260+ tokens 全部定义 |
| Semantic Tokens | 85% | 核心 tokens 完成，需补充玻璃态等 |
| Component Tokens | 60% | Button/Slider 完成，其他待迁移 |
| 组件实现 | 40% | 227 处硬编码待优化 |

---

## 🚀 下一步建议

### 短期目标（1-2 周）
1. 补充玻璃态相关 semantic tokens
2. 优化 ViewportArea.tsx（68 处硬编码）
3. 创建通用 CSS 类库

### 中期目标（1 个月）
1. 迁移所有面板组件
2. 统一所有颜色使用
3. 完善文档和示例

### 长期目标（持续）
1. 建立 Storybook 文档
2. 添加主题切换功能
3. 性能优化和监控

