/**
 * 🎨 Theme Integration Layer
 * 从 @genki/shared-theme 导入并重新导出 tokens
 */

export { BASE_TOKENS, THEME_TOKENS } from '@genki/shared-theme';
export type { TokenState } from '@genki/shared-theme';
